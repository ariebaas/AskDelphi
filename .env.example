# ============================================================================
# DIGITALECOACH CLIENT - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (it's in .gitignore)
#
# QUICK START:
# 1. Choose your authentication mode (cache or traditional)
# 2. Fill in the required fields for your mode
# 3. Set ASKDELPHI_BASE_URL to your production API server
# 4. Run: python main.py
#
# ============================================================================
# AUTHENTICATION MODE (REQUIRED)
# ============================================================================
# Choose ONE of these modes:
#
# "cache" = RECOMMENDED for production
#   - Uses CMS URL + Portal Code (token caching mechanism)
#   - Tokens are cached locally after first login
#   - More secure, no API key needed
#
# "traditional" = For testing/legacy systems
#   - Uses API Key + Tenant/Project/ACL
#   - Less secure, API key stored in .env
#
ASKDELPHI_AUTH_MODE=cache

# ============================================================================
# CACHE MODE CONFIGURATION (if ASKDELPHI_AUTH_MODE=cache)
# ============================================================================

# ASKDELPHI_CMS_URL (REQUIRED)
# The full URL from your browser when viewing a Digital Coach page
# Steps to find it:
#   1. Open AskDelphi in your browser
#   2. Navigate to your Digital Coach page
#   3. Copy the full URL from the address bar
#   4. Format: https://company.askdelphi.com/cms/tenant/{ID}/project/{ID}/acl/{ID}/...
#
# EXAMPLE: https://company.askdelphi.com/cms/tenant/abc-123/project/def-456/acl/ghi-789/page
ASKDELPHI_CMS_URL=https://company.askdelphi.com/cms/tenant/YOUR_TENANT_ID/project/YOUR_PROJECT_ID/acl/YOUR_ACL_ID/page

# ASKDELPHI_PORTAL_CODE (REQUIRED for first login only)
# One-time code for initial authentication
# Steps to find it:
#   1. Open AskDelphi mobile app or web interface
#   2. Go to Settings → Mobile tab
#   3. Copy the Portal Code (format: ABC123-XYZ789)
#   4. After first successful login, tokens are cached and this is no longer needed
#
# IMPORTANT: This code is ONE-TIME USE ONLY. Get a new one if authentication fails.
ASKDELPHI_PORTAL_CODE=YOUR_PORTAL_CODE_HERE

# ============================================================================
# TRADITIONAL MODE CONFIGURATION (if ASKDELPHI_AUTH_MODE=traditional)
# ============================================================================
# ONLY uncomment and fill these if using traditional mode
# For production, cache mode is STRONGLY RECOMMENDED
#
# ASKDELPHI_API_KEY=your-actual-api-key-here
# ASKDELPHI_TENANT=your-tenant-id
# ASKDELPHI_PROJECT_ID=your-project-id
# ASKDELPHI_ACL=your-acl-id

# ============================================================================
# REQUIRED FOR ALL MODES
# ============================================================================

# ASKDELPHI_BASE_URL (REQUIRED)
# The base URL for API calls
# - For production: https://your-company.askdelphi.com/api
# - For testing: http://127.0.0.1:8000
# - Extract from CMS_URL if unsure (everything before /cms/)
#
ASKDELPHI_BASE_URL=https://your-company.askdelphi.com/api

# ASKDELPHI_NT_ACCOUNT (REQUIRED)
# Your user identifier for audit trail and authorization
# Format: username or domain\username
# Example: john.doe or COMPANY\john.doe
#
ASKDELPHI_NT_ACCOUNT=your-username-here

# ============================================================================
# PORTAL & API SERVER CONFIGURATION (OPTIONAL)
# ============================================================================
# These URLs are used for authentication and API token retrieval
# Default values point to production AskDelphi servers
# Override only if using a custom or on-premise installation
#
# ASKDELPHI_PORTAL_SERVER (OPTIONAL)
# Portal server for authentication
# Default: https://portal.askdelphi.com
# ASKDELPHI_PORTAL_SERVER=https://portal.askdelphi.com

# ASKDELPHI_API_SERVER (OPTIONAL)
# API server for token retrieval
# Default: https://edit.api.askdelphi.com
# ASKDELPHI_API_SERVER=https://edit.api.askdelphi.com

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# DEBUG (true/false)
# Enable detailed logging for authentication and API calls
# Useful for troubleshooting connection issues
DEBUG=false

# SKIP_CHECKOUT_CHECKIN (true/false)
# Whether to skip the checkout/checkin workflow when updating topics
# - true = faster, no locking (default)
# - false = safer, uses checkout/checkin for concurrent edit protection
SKIP_CHECKOUT_CHECKIN=true

# USE_AUTH_CACHE (true/false)
# Explicitly enable/disable token caching
# Normally auto-detected (disabled for localhost, enabled for production)
# Uncomment only if you need to override auto-detection
# USE_AUTH_CACHE=true
